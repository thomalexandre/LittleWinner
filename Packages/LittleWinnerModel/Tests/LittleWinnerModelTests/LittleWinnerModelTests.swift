import XCTest
@testable import LittleWinnerModel

final class LittleWinnerModelTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(LittleWinnerModel().text, "Hello, World!")
    }
}
